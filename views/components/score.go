package components

// ScoreExplainer is the shared explanation of the performance-score
// ranking formula, revealed via a .score-info-btn/.score-info-text toggle
// (static/js/main.js) wherever the score is shown — the group leaderboard
// and a player's own profile page.
const ScoreExplainer = "Ranked by performance score: (3×Wins + Draws + Goals + Assists + 2×Clean Sheets for Goalkeepers/Defenders) ÷ Matches. Players are ranked by wins until they've played 3 matches, then by score like everyone else."
